using namespace std;

long binary_search(long left, long right){ //左右でTorFを分けるもの
    while(right - left > 1){
        long mid = (left + right) / 2; //左寄り
        //long mid = (left + right + 1) / 2; //右寄り

        //if() left = mid; //
        //else right = mid; //

    }
}